function rmsandbox()
    if ~isempty( ver( 'mcam' ) )
        fx.mcam.command.rmsandbox();
    else
        thisPath = fileparts( mfilename( 'fullpath' ) );
        rmpath( fullfile(...
            thisPath,...
            'code',...
            'fxgraphics' ) );
        rmpath( fullfile(...
            thisPath,...
            'test' ) );
        rmpath( fullfile(...
            thisPath,...
            'demo' ) );
    end
end